export interface SegmentChart {
  labels: string[];
  dataChart: number[];
}

export interface SegmentCriteria {
  uuid: string;
  name: string;
}

export interface SegmentType {
  uuid: string;
  name: string;
}
